import { Meta } from '@storybook/addon-docs';

<Meta title="Design / Button" />

# Button 🧱🤖

Дизайнер рисует кнопки проекта на фрейме `Button` на странице `Ui-kit`.

Должно быть отрисовано:

1. Все темы в различных состояниях
1. Размеры под основную тему

<img src="images/buttons/frame.png" alt="Фрейм Button" width="700" />

_Фрейм Button_

Все остальные вариации будут созданы на фронтенде автоматически: блочные кнопки, иконочные, многострочные, сочетания каждой темы с размерами и т.д. Они доступны для демонстрации через автокит (смотрите автокит `Button`).

<img src="images/buttons/result.png" alt="Доступные вариации" width="700" />

_Доступные вариации_

Ничто не мешает отрисовать и другие вариации. Например, блочную и многострочную, чтобы использовать их в макетах.

Также отдельно нужно отрисовать круглую иконочную кнопку, если для вариации без текста нужно применить полное скругление.

## Themes

**Тема** — это единое стилистическое решение в нескольких состояниях.

🏆 `primary` и `secondary` – хорошие названия для тем.

Всего доступно 5 состояний:

1. `default` – обычное
1. `hover` – при наведении
1. `active` – при нажатии
1. `disabled` – заблокировано
1. `focus` – фокус с клавиатуры

Обязательными для определения дизайнером являются 3 из них: **default**, **hover**, **disabled**.

Состояние **active** по умолчанию совпадает с **hover**, а **focus** наследует состояние фокуса всего проекта (обычно прямоугольный бокс).

## Sizes

Дизайнер рисует размеры на примере основной темы. Эти размеры будут применены ко всем темам. Обычно их три, где средняя является основной, но технических ограничений на количество нет.

🏆 `lg`, `md` и `sm` – хорошие названия для размеров.

При изменении размера в кнопке могут меняться следующие стили:

1. Типографика
1. Высота
1. Горизонтальные поля
1. Размер иконки
1. Отступ между иконкой и текстом

Кнопки делаются в Auto Layout.

Поскольку Auto Layout может автоматически менять ширину, в кнопку лучше сразу положить иконку слева и справа от текста и скрыть. Это позволит гибко использовать компонентный подход.

Иконки и текст выравниваются по вертикальному центру, тексту задаётся стиль из типографики.

В настройках задаётся фиксированная высота, горизонтальные отступы и отступ между внутренними элементами.

<img src="images/buttons/auto-layout.png" alt="Панель Auto Layout в Figma" width="400" />

_Панель Auto Layout в Figma_

Блочные кнопки делаются по старому принципу без Auto Layout.

Для задания типографики используются стили из фрейма `Typography`. Обычно это отдельные переменные под каждый размер без `mobile` версии (т.е. статичные).

На примере GDS:

1. `buttonLg` – большая
1. `buttonMd` – средняя
1. `buttonSm` – малая
