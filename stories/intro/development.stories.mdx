import { Meta } from '@storybook/addon-docs';

<Meta title="Intro / Development Guide" />

# Development Guide

Информация на этой странице предназначена для разработчиков GDS.

## Scripts

| `yarn <command>`  | Описание                                                           |
| ----------------- | ------------------------------------------------------------------ |
| `build`           | Выполняет полную сборку пакета с очисткой, сборкой бандлов и типов |
| `build:main`      | Параллельно собирает бандлы и типы. Используется в `build`         |
| `build:rollup`    | Собирает бандл через rollup. Используется в `build:main`           |
| `build:types`     | Собирает типы через tsc. Используется в `build:main`               |
| `clear`           | Очищает директории сборки                                          |
| `deploy`          | Собирает Storybook и выгружает на Github Pages                     |
| `publish`         | Упаковывает и размещает в npm                                      |
| `eslint`          | Линтинг `.js`, `.ts`, `.tsx` и `.mdx` файлов                       |
| `lint-staged`     | `lint-staged` добавлен в скрипты для использования с `run-p`       |
| `prepublishOnly`  | Хук, выполняющий сборку при публикации через `npm publish`         |
| `storybook`       | Запускает Storybook в dev режиме                                   |
| `storybook:build` | Собирает статическую версию Storybook в `docs`                     |
| `storybook:serve` | Запускает сервер для просмотра статической версии                  |
| `tokens`          | Обновляет токены Figma из основного проекта                        |
| `scss-tokens`     | обновляет SCSS-файлы, используя предоставленные командой `tokens`  |
| `tokens:dev`      | Обновляет токены Figma из development проекта                      |
| `tsc`             | Проверка Typescript (noEmit)                                       |

## Docs

В документации использовать зафиксированный здесь список emojis:

-   ✅ – успех, «do»
-   ⛔ – ошибка, «don't»
-   ❗️ – важное замечание
-   💬 – сноска
-   ✨ – дизайн–токены
-   🧱 – компоненты
-   ⚙️ – утилиты
-   🤖 – автокиты
-   🔥 - ядро, возможности
-   🏆 – рекомендации, лучшие практики
-   ♿️ – accessibility
-   ⏱ – performance
-   📚 – ссылки на статьи
-   🔨 – конфигурация
-   🚧 – в разработке

Все заголовки в документации писать на английском, т.к. на кириллических не работают ссылки.
