import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Intro / Development Guide" />

# Development Guide

Информация на этой странице предназначена для разработчиков GDS.

**Список команд:**

| `yarn <command>`  | Описание                                          |
| ----------------- | ------------------------------------------------- |
| `storybook`       | Запускает Storybook в dev режиме                  |
| `storybook:build` | Собирает статическую версию Storybook в `docs`    |
| `storybook:serve` | Запускает сервер для просмотра статической версии |
| `tokens`          | Обновляет токены Figma из основного проекта       |
| `tokens:dev`      | Обновляет токены Figma из development проекта     |
| `deploy`          | Собирает Storybook и выгружает на Github Pages    |
| `build`           | Собирает пакет на rollup                          |
| `eslint`          | Линтинг `.js`, `.ts` и `.tsx` файлов              |

**Список emojis:**

-   ✅ – успех, «do»
-   ⛔ – ошибка, «don't»
-   ❗️ – важное замечание
-   💬 – сноска
-   ✨ – дизайн–токены
-   🧱 – компоненты
-   ⚙️ – утилиты
-   🤖 – автокиты
-   🔥 - ядро, возможности
-   🏆 – рекомендации, лучшие практики
-   ♿️ – accessibility
-   ⏱ – performance
-   📚 – ссылки на статьи
-   🔨 – конфигурация
-   🚧 – в разработке
