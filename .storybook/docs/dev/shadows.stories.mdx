import { Meta } from '@storybook/addon-docs/blocks';
import Storybook from '../../../src/components/Storybook';

<Meta title="Dev | Shadows" />

# Shadows

<Storybook.Shadows />

Работа с тенями полностью аналогична работе с цветами.

Тени тянутся из Figma через токены, доступны в объекте темы через поле `shadows` и представляются через автокит `Shadows`.
