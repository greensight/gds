import { Meta } from '@storybook/addon-docs/blocks';
import Note from '../../components/Note';
import Grid from '../../components/Grid';
import DoDont from '../../components/DoDont';
import lockColor from '../../../src/images/lock-color.jpg';
import basicBlue from '../../../src/images/basic-blue.jpg';
import aboutHSL from '../../../src/images/about-hsl.jpg';
import colorDesignerLight from '../../../src/images/color-designer-light.jpg';
import stackLight from '../../../src/images/stack-light.jpg';
import colorDesignerDark from '../../../src/images/color-designer-dark.jpg';
import stackDark from '../../../src/images/stack-dark.jpg';
import stackFull from '../../../src/images/stack-full.jpg';
import structurePalettes from '../../../src/images/structure-palettes.jpg';
import structureColors from '../../../src/images/structure-colors.jpg';
import textFrame from '../../../src/images/text-frame.jpg';

<Meta title="Design | Colors" />

# Colors

## Составление палитр

<br />
<Note icon="🤝🏿">
    Между отделами фронтенда и UX заключено перемирие соглашение, которые упрощает жизнь и делает осознанным выбор цвета
    на проекте. Дизайнеры получают рабочую систему подбора палитр, а фронтендеры — менее «50-ти оттенков серого». Вместе
    мы ускоряем рабочий процесс и поддерживаем порядок в проекте.
</Note>

В большинстве проектов за последние годы клиенты приходят к нам со своими цветами в логотипе или брендбуке. Твоя задача — не трогать основные цвета и построить палитру оттенков.

<Note icon="🙋🏿‍♀️">
    — Позвольте! А что же делать, если клиент не принес цветов?
    <br />— Подарите ему их сами! Ведь вы же дизайнер :)
</Note>
<br />
<Note icon="👉🏿">
    Предлагаемые варианты — не панацея. Ты можешь построить палитру по своему усмотрению. Проследи, чтобы главные цвета
    от клиента остались неизменными.
</Note>
<br />

### Автоматизированный процесс построения палитры

Инструмент [ColorBox](https://www.colorbox.io/) от Lyft Design.

Гибкий и интуитивно понятный инструмент для создания цветовых палитр.

Не забудь зафиксировать цвет, который дал клиент:

<img
    src={lockColor}
    alt="Фиксируем цвет #EA1414 в Lock Color"
    width={400}
    style={{ display: 'block', margin: '0 auto' }}
/>

### Как самостоятельно построить палитру основных цветов

<Grid>
    <Grid.Item>
        Для примера мы возьмем синий цвет и рассмотрим его параметры в модели HSL: (208, 100, 34, 100).
        <br />
        <br />
        <img src={basicBlue} alt="Квадрат выбранного синего цвета" />
    </Grid.Item>
    <Grid.Item>
        <div>
            HSL — цветовая модель, в основе которой лежат три параметра: Hue (оттенок), Saturation (Насыщенность)
            и Lightness (Светлота).
        </div>
        <br />
        <br />
        <img src={aboutHSL} alt="Демонстрация HSL" />
    </Grid.Item>
</Grid>

Построим палитру из 10-ти цветов с помощью плагина Color Designer. Параметр L у нашего цвета равен 34 из 100. Значит в палитре по светлости он будет третьим. Нужно сделать 7 цветов в сторону светлых оттенков и 2 в сторону темных.

#### Делаем светлые оттенки

<Grid>
    <Grid.Item>
        <img src={colorDesignerLight} alt="Подбор светлых оттенков в Color Designer" />
    </Grid.Item>
    <Grid.Item>
        Последний цвет в палитре плагина всегда белый. Мы его не берем, нам нужны предыдущие семь. Их мы переносим в
        подготовленные прямоугольники.
        <br />
        <br />
        <img src={stackLight} alt="Получившийся стек светлых оттенков от 3 до 9" />
    </Grid.Item>
</Grid>

#### Делаем темные оттенки

<Grid>
    <Grid.Item>
        <img src={colorDesignerDark} alt="Подбор темных оттенков в Color Designer" />
    </Grid.Item>
    <Grid.Item>
        Последний цвет в этом случае всегда черный и его мы тоже не берем. Остальные переносим.
        <br />
        <br />
        <img src={stackDark} alt="Получившийся стек темных оттенков от 0 до 1" />
    </Grid.Item>
</Grid>

#### Корректировка оттенков

<Grid>
    <Grid.Item>
        Скорректируй оттенки по насыщенности — этим управляет параметр S. Простое правило: чем темнее цвет, тем больше
        насыщенность и наоборот. Не забывай про глаз, его не заменят набор правил.
        <br />
        <br />
        После корректировки ты получишь цветовую палитру из 10 оттенков и выполнишь главное условие: цвет, который пришел
        от клиента, остался неизменным.
    </Grid.Item>
    <Grid.Item>
        <img src={stackFull} alt="Получившийся стек от 0 до 9" />
    </Grid.Item>
</Grid>

## Система токенов

Мы настроили автоматический перенос цветов в Storybook проекта. За это отвечают фреймы `Palettes` и `Colors`. Предложенное решение масштабируется под любое количество цветов, но их необходимость в проекте должна быть обоснована.

`Palettes` хранит палитры, во фрейме `Colors` дизайнер присваивает цветам переменные и разбивает их по функциям. Каждый цвет имеет одну или несколько функций. Значения цветов переменных подбираются из фрейма `Palettes`.

<Note icon="⛔">Дизайнер не меняет структуру слоев и подход к именованию слоев во фреймах.</Note>
<br />
<Note icon="⛔">Дизайнер не меняет имена переменных после экспорта цветов в Storybook.</Note>

### Структура слоёв Palettes

<img src={structurePalettes} alt="Пример структуры фрейма Palettes" />

Вложенные фреймы `blue` и `red` содержат палитры основных цветов проекта. В GDS эти цвета использованы для примера.

Мы можем добавить от 10 до 15 оттенков каждого цвета. Они именуются по названию оттенка, который в себе содержат.

Во фрейме `grey` лежит палитра оттенков серого. Она изначально содержит 15 оттенков, чтобы захватить самые светлые участки.

Каждый цвет в палитре должен быть пронумерован. Нумерация всегда начинается с нуля.

### Структура слоёв Colors

<img src={structureColors} alt="Пример структуры фрейма Colors" />

Глобальные переменные `text`, `bg`, `borders`, `function` присутствуют всегда.

Внутри может быть любое разумное количество цветов. Дизайнер предполагает, в какой из четырех функций используется цвет, добавляет фрейм и дает название переменной.

Название переменной пишется текстом на цвете. В слоях ничего не переименовывается.

<img src={textFrame} alt="Визуальное представление фрейма text" />

Чтобы лучше понимать, где цвет используется, внизу есть описание. Заполнять его не обязательно.

### Нейминг

Задача дизайнера не только подобрать цвета, но и присвоить им переменные — _задекларировать_. Это делается во фрейме `Colors`.

Для проекта нам требуется минимальные функциональные переменные — они уже прописаны в GDS. Остальные переменные используют формат [camelCase](https://ru.wikipedia.org/wiki/CamelCase).

Мы не используем транслитерацию — все слова пишутся на английском.

Первое слово переменной начинается со строчной, каждое последующее — с прописной. Несколько слов пишутся слитно без пробела, дефиса или нижнего подчеркивания.

Идеальные имена переменных не содержат привязки к семантике и не используют в обозначениях названия цветов.

<DoDont>
    <DoDont.Dont>
        h2Grey
        <br />
        paragraphBlue70
        <br />
        Black_button
    </DoDont.Dont>
    <DoDont.Do>
        titlePromo
        <br />
        link
        <br />
        dark
    </DoDont.Do>
</DoDont>

### Начальный состав переменных

#### Тексты (text)

`link` — ссылки, фирменный

`linkHover` —  ссылки, фирменный при наведении

`default` — основной

`second` — дополнительный

`disabled` — для заблокированных элементов

`lighter` — инверсионный

#### Фоны (bg)

`dark` — темный

`second` —  серый

`lighter` — светлый

`disabled` — для заблокированных элементов

`brand` — главный фирменный цвет

`brandHover` — главный фирменный цвет при наведении

`brandSecond` — второстепенный фирменный цвет

`brandSecondHover` — второстепенный фирменный цвет при наведении

#### Рамки (borders)

`light` — светлые

`second` —  дополнительные

`brand` — фирменные

`dark` — темные

`disabled` — для заблокированных элементов

#### Функции (functions)

`error` — ошибки

`warning` —  предупреждения

`success` — успешные действия

`info` — информация
