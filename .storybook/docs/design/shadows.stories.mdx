import { Meta } from '@storybook/addon-docs/blocks';
import Note from '../../components/Note';
import DoDont from '../../components/DoDont';
import shadowsStructure from '../../../src/images/docs/shadows-structure.png';
import shadowsBlocks from '../../../src/images/docs/shadows-blocks.png';
import shadowsFrame from '../../../src/images/docs/shadows-frame.png';
import shadowsEffects from '../../../src/images/docs/shadows-effects.png';

<Meta title="Design / Shadows" />

# Shadows

Фрейм `Shadows` отвечает за автоматический экспорт стилей теней из Figma в Storybook.

## Структура слоев

<br />
<img src={shadowsStructure} style={{ display: 'block', margin: '0 auto' }} />
<Note icon="❌">Дизайнер не меняет структуру слоев и подход к именованию слоев во фреймах.</Note>
<Note icon="❌">Дизайнер не меняет имена переменных после экспорта стилей теней в Storybook.</Note>

## Создание дополнительных теней

Внутри фрейма может быть любое разумное количество стилей теней. На примере GDS:

<img src={shadowsBlocks} style={{ display: 'block', margin: '0 auto' }} />

_Блоки со стилями_

<img src={shadowsFrame} style={{ display: 'block', margin: '0 auto' }} />

_Структура слоев_

Дизайнер предполагает, где в проекте используется тень, добавляет фрейм и даёт название переменной.

<Note icon="👉🏿">Название переменной пишется текстом на блоке. В слоях ничего не переименовывается.</Note>

## Нейминг

Задача дизайнера не только подобрать тени, но и присвоить им переменные.

Для проекта нам требуется минимальные функциональные переменные — они уже прописаны в GDS — `small`, `big`, `inner`. дополнительные переменные используют формат [lowerCamelCase](https://ru.wikipedia.org/wiki/CamelCase).

Мы не используем транслитерацию — все слова пишутся на английском.

Первое слово переменной начинается со строчной, каждое последующее — с прописной. Несколько слов пишутся слитно без пробела, дефиса или нижнего подчеркивания.

Дизайнер следит, чтобы переменные были очевидными, понятными и универсальными.

<DoDont>
    <DoDont.Dont>
        shadow1
        <br />
        SmallShadow
        <br />
        super-inside-btn
    </DoDont.Dont>
    <DoDont.Do>
        big
        <br />
        small
        <br />
        inner
    </DoDont.Do>
</DoDont>

## Стили Фигмы

Тени заносятся в стили Фигмы и переиспользуются в дальнейших макетах проекта.

<img src={shadowsEffects} style={{ display: 'block', margin: '0 auto' }} />
