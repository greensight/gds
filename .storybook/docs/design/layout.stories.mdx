import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Design / Layout" />

# Layout

Фрейм `Layout` отвечает за передачу в Storybook данных о сетке проекта и определяет, как эта сетка будет использоваться.

![Фрейм Layout](images/layout/frame.png)

Проект содержит девять брейкпоинтов, на которых возможны изменения в сетке. Кол-во брейкпоинтов не означает физическую отрисовку макетов под каждый, они передают переменные под каждое разрешение.

**Каждый брейкпоинт содержит переменные:**

-   `cols` - кол-во колонок на брейкпоинте
-   `gap` - отступ между колонками
-   `padding` - отступ от контейнера до края макета

Также в сторибук передается ширина брейкпоинтов и ширина контейнера, который выравнивается относительно страницы и имеет фиксированную ширину на больших экранах.

## Что меняет дизайнер

Дизайнер меняет значения брейкпоинтов, кол-во колонок и размеры отступов, если того требует проект. В этом случае он выстраивает сетку в соответствии с примером из ГДС.

Система автоматически передает в сторибук данные. Названия слоев и значения внутри не важны. Но, поскольку значения берутся из фактического размера прямоугольников и фреймов, дизайнер следит, чтобы границы фреймов соответствовали значению брейкпоинта.

![Корректная отрисовка брейкпоинта](images/layout/breakpoint-good.png)

Правильно: размер фрейма соответствует брейкпоинту

![Некорректная отрисовка брейкпоинта](images/layout/breakpoint-bad.png)

Неправильно: размер фрейма больше брейкпоинта

Стили сетки в Фигме должны соответствовать тому, что нарисовано на брейкпоинте:

![Соответствие брейкпоинта сетке](images/layout/breakpoint-grid.png)

## Поведение контейнера на больших экранах

Самый большой брейкпоинт (в ГДС — 1920 px) определяет его поведение на больших разрешениях. В примере ГДС контейнер фиксируется по ширине и выравнивается по центру.

👉🏿 В случае, если ширина контейнера фиксируется, в параметре **padding** дизайнер указывает значение **auto**.

Дизайнер может выровнять контейнер по левому краю или сделать полностью резиновый макет.

![Центрированный контейнер](images/layout/container-center.png)

Для фиксированного контейнера с выравниваем по центру указываются отступы со значением «auto»

![Боковой контейнер](images/layout/container-left.png)

Для выравнивания фиксированного контейнера по левому краю слева остается отступ в пикселях, справа — auto

![Без контейнера](images/layout/container-none.png)

Для полностью резинового макета на любом разрешении значение auto не указывается

Частные случаи с фиксированными сайдбарами обсуждаются с разработчиком отдельно.

## Нестандартные колонки и блоки

Система позволяет строить сетки в автоматическом режиме. На примере 12 колоночной сетки мы можем сделать блок с 5, 8 или 10 колонками:

![Автоматические сетки](images/layout/auto.png)

Система позволяет строить сетку внутри колонок. Нарисуем нестандартный блок:

![Нестандартная сетка. Шаг 1](images/layout/uncommon-grid-1.png)

На первый взгляд, он не попадает в сетку, но рассмотрим его внимательнее — левая колонка занимает ровно 1/5 от ширины блока:

![Нестандартная сетка. Шаг 2](images/layout/uncommon-grid-2.png)

Если разбить этот контейнер на две части, справа получится 4/5:

![Нестандартная сетка. Шаг 3](images/layout/uncommon-grid-3.png)

В правой части можно создать фрейм и задать ему свою систему колонок, например 6:

![Нестандартная сетка. Шаг 4](images/layout/uncommon-grid-4.png)

По этим колонкам можно построить дополнительные блоки:

![Нестандартная сетка. Шаг 5](images/layout/uncommon-grid-5.png)

👉🏿 Важно помнить, что на брейкпоинте используется свой параметр **gap** — ширина отступа между колонками. Отступ между колонками должен соответствовать этому значению, в нашем случае — 24 px.

Таким образом мы получаем нестандартный блок, который разработчик сможет быстро воспроизвести в коде.
