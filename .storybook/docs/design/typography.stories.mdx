import { Meta } from '@storybook/addon-docs/blocks';
import Storybook from '../../../src/components/Storybook';
import Note from '../../components/Note';
import Grid from '../../components/Grid';
import typographyFrame from '../../../src/images/docs/typography-frame.png';
import structureTypography from '../../../src/images/docs/structure-typography.png';
import structureBreakpoints from '../../../src/images/docs/structure-breakpoints.png';
import typographyVars from '../../../src/images/docs/typography-vars.png';
import multipleStyles from '../../../src/images/docs/multiple-styles.png';
import lineHeight from '../../../src/images/docs/line-height.png';
import paragraphsOffset from '../../../src/images/docs/paragraphs-offset.jpg';

<Meta title="Design | Typography" />

# Typography

Фрейм `Typography` Figma хранит данные о типографике проекта, которые синхронизируются с проектом через дизайн-токены. Решение диктует строгость в использовании стилей, экономит время разработки и упрощает корректировку значений.

<img src={typographyFrame} alt="Внешний вид фрейма Typography в Figma" />

## Задача дизайнера

Дизайнер подбирает типографику и меняет параметры по предложенному шаблону. Новые переменные добавляются в конец списка, соблюдая оригинальную вложенность и именование слоев. Из подобранных значений дизайнер создает текстовые стили в Фигме и использует их на проекте.

## Что передается в Storybook

1. font-family
1. font-style
1. font-weight
1. font-size
1. line-height
1. letter-spacing
1. text-transform
1. text-decoration
1. font-variant-numeric
1. breakpoints

## Структура слоёв в Фигме

Синим цветом обозначены слои, в которые дизайнер вносит изменения:

<img src={structureTypography} alt="Структура фрейма Typography" />

**Description** - вспомогательный фрейм для подписей, не выносится в Storybook.

### Breakpoints

**Breakpoints** - в Storybook уходят названия слоев из фрейма `breakpoints`. Дизайнер задает начальную и конечную точки для плавного скэйлинга размера шрифтов. Для GDS это **1440** и **768** px.

Дизайнер самостоятельно меняет названия слоев, чтобы изменить значения:

<img src={structureBreakpoints} alt="Структура фрейма Breakpoints" />

При этих значениях шрифты будут плавно изменять размер только в заданном диапазоне.

**При разрешении экрана 1920** пользователю отобразятся размеры из набора фрейма Desktop.
**При разрешении экрана 375** пользователю отобразятся размеры из набора фрейма Mobile.
**При разрешении экрана 960 или 1280** пользователю отобразятся автоматически подобранные значения в интервале между **768** и **1440**.

<Note icon="👉🏿">Брейкпоинтов не может быть больше двух</Note>

### Desktop и Mobile

Переменные и их значения указываются во фреймах `Desktop` и `Mobile`. В нашем примере Desktop соответствует брейкпоинту **1440**, а Mobile — **768**.

Для проекта нам требуется минимальные типографические переменные — они уже созданы в GDS:

-   h1, h2, h3, h4, h5, h6 — стили заголовков
-   subheading — подзаголовки
-   body — основной текст
-   small — мелкие тексты
-   caption — подписи
-   button — тексты на кнопках

Названия переменных во фреймах Desktop и Mobile должны совпадать. Допускается, что во фрейме Mobile отсутствуют переменные,
которые есть в Desktop. В этом случае у них не будет скэйлинга. В GDS переменные caption и button имеют одинаковый
размер на мобильных и десктопах, поэтому скэйлинг к ним не применяется.

Структура переменных в слоях GDS:

<br />
<img src={typographyVars} alt="Именование элементов фрейма"
width={400}
style={{ display: 'block', margin: '0 auto' }}/>
<br />
<Note icon="⛔">Дизайнер не меняет структуру слоев и подход к именованию новых слоев во фрейме</Note>

## Новые переменные

Дизайнер добавляет новые переменные в конец списка, если это обосновано.

Для нейминга используется формат [lowerCamelCase](https://ru.wikipedia.org/wiki/CamelCase): первое слово переменной начинается со строчной, каждое последующее — с прописной. Несколько слов пишутся слитно без пробела, дефиса или нижнего подчеркивания.

Мы не используем транслитерацию — все слова пишутся на английском.

## Похожие стили

Для одной функции могут использоваться несколько похожих стилей, незначительно отличающихся друг от друга. Допускается записывать их на одной строке:

<img src={multipleStyles} alt="Несколько стилей для одной функции" />

Для таких дополнительных стилей нужно соблюдать логику и принципы именования слоев:

<Note icon="👉🏿">
    body → bodyBold
    <br />
    small → smallBold
    <br />
    caption → captionUppercase
</Note>

В структуре слоев они располагаются сразу за основными стилями.

## Рекомендации для дизайнера

-   Указывай line-height в процентах. Это позволит одинаково выставлять значения для разных кеглей и не менять значение при смене кегля. Разумно использовать значение, кратное десяти: 120%, 130%, 140%, 150% и так далее.
-   Не используй line-height в качестве отступов при построении вертикальных списков. Это рушит логику работы с текстом, а при переносе строки мы имеем неадекватный line-height. Это мешает фронтенду выставить точные показатели. Это нарушает работу со стилями.

<img src={lineHeight} alt="Пример выравнивания через некорректный line-height" />

Правильнее в этом случае использовать отступ между абзацами:

<img src={paragraphsOffset} alt="Пример выравнивания через отступы параграфов" />

line-height в текстовом блоке с ссылками 32 px. Оригинальный — 24 px.

-   Не используй больше двух разных нестандартных шрифтов в проекте, если это ничем не оправдано. Скорость загрузки страницы увеличивается пропорционально количеству начертаний.
-   Подбирай фоллбеки для нестандартных шрифтов. Фоллбек — системный шрифт, который видит пользователь, пока не загрузится нестандартный шрифт. Для уменьшения визуального скачка подбирай похожий по параметрам шрифт из системных.

## Ресурсы для построения ритма кегля

Мы рекомендуем инструменты, которые помогут дизайнеру спроектировать равномерный ритм размера шрифта в проекте. Сервисы — не панацея, а инструмент помощи и системный подход к построению типографики.

-   [Modular Scale](https://www.modularscale.com/)
-   [A Visual Type Scale](https://type-scale.com/)
