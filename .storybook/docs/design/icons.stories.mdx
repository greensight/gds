import { Meta } from '@storybook/addon-docs/blocks';
import Note from '../../components/Note';
import DoDont from '../../components/DoDont';
import iconsStructure from '../../../src/images/docs/icons-structure.png';
import iconsUnion from '../../../src/images/docs/icons-union.png';
import iconsSize from '../../../src/images/docs/icons-size.png';
import iconsForm from '../../../src/images/docs/icons-form.png';
import iconsNames from '../../../src/images/docs/icons-names.png';
import iconsGroup from '../../../src/images/docs/icons-group.png';
import iconsStandart from '../../../src/images/docs/icons-standart.png';

<Meta title="Design | Icons" />

# Icons

Фрейм `Icons` отвечает за автоматический экспорт иконок из Figma в Storybook. Иконки экспортируются в квадратах указанных размеров.

Каждая иконка состоит из векторных слоев и обёртки-компонента.

<br />
<img src={iconsStructure} style={{ display: 'block', margin: '0 auto' }} />
<Note icon="❗️">
    Иконки должны быть сделаны с использованием параметра fill. Использовать оутлайновые иконки с параметром stroke
    нельзя.
</Note>

Строковые слои можно объединить в Union Selection и не применять к ним Outline Stroke или Flatten. Union на экспорте обеспечит иконке правильную заливку, оставляя оригинал иконки внутри себя:

<img src={iconsUnion} style={{ display: 'block', margin: '0 auto' }} />

При дальнейшем редактировании к оригиналу всегда можно вернуться и что-либо подправить. Это работает только для областей с одним цветом заливки.

## Группировка и размеры

<Note icon="❗️">
    Иконка должна заключаться в строгий квадрат — это гарантирует правильное отображение иконки в макетах и после
    разработки.
</Note>

Иконки разбиваются на группы в зависимости от места, где будут использоваться. Название группы в Фигме не влияет на сторибук, важно чтобы группа называлась интуитивно понятно.

В GDS использованы названия `small`, `medium` и `large`, которые соответствуют квадратам 12, 24 и 32 px. Дизайнер может создать группу `catalog`, в которой будут лежать иконки для каталога.

Размеры квадратов всегда соответствуют системе спейсингов:

<br />
<img src={iconsSize} style={{ display: 'block', margin: '0 auto' }} />
<Note icon="👉🏿">
    Практически в любой системе можно обойтись максимум тремя размерами иконок. Если их больше — дизайнер должен
    обосновать свой подход.
</Note>

## Проектирование

Иконки рисуются на базе 24-х пиксельного квадрата, имеют форму прямоугольника или круга:

<img src={iconsForm} style={{ display: 'block', margin: '0 auto' }} />

Строго соблюдать сетку выше не следует, нужно больше ориентироваться на визуальную оценку. Сетка лишь дает подсказку.

## Нейминг

Кроме отрисовки иконок в задачу дизайнера входит их структурирование. На основе аналитики мы выделили структуру слоев и назначили нейминг:

<img src={iconsNames} style={{ display: 'block', margin: '0 auto' }} />

`size` — группа, в которую входит иконка. Допустимо использовать цифру из стороны квадрата (24), иные обозначения размеров (medium, small) или произвольное название для группы.

`name` — уникальное название иконки, по которой ее можно будет легко найти.

При указании имен используется формат lowerCamelCase.

<img src={iconsGroup} style={{ display: 'block', margin: '0 auto' }} />

Важно именовать иконки в соответствии с тем, что на них нарисовано, на правильном английском и без транслитерации. Неправильно именовать иконку по ее действию или метафоре, поэтому:

<DoDont>
    <DoDont.Dont>
        idea
        <br />
        closed
        <br />
        strelkaGalkaRight
    </DoDont.Dont>
    <DoDont.Do>
        lamp
        <br />
        cross
        <br />
        shevronRight или shevron/right
    </DoDont.Do>
</DoDont>

## Стандартная коллекция

На странице `Blanks` во фрейме `Collection` находятся иконки, которые повторяются в каждом проекте и выглядят одинаково.

<img src={iconsStandart} style={{ display: 'block', margin: '0 auto' }} />

Приветствуется пополнение стандартной коллекции GDS для ускорения процесса работы над макетами.

Для упрощения дальнейшей стилизации иконки отрисованы оутлайнами в размере 24×24 px. Желательно придерживаться этого правила при добавлении новых иконок.

## Рекомендации

1. Хорошо когда в ките одинаковая логика иконок: стиль, толщина линий, цвета, размеры, расположение, скругление углов.
2. Нужно занести в кит максимальное кол-во используемых иконок. Это ускорит процесс разработки.
3. Иконки, выполняющие роль иллюстраций или подчеркивающие стиль могут не подчиняться правилам и не заноситься в кит.
4. Одноцветные иконки состоят из одного слоя, если это возможно.
5. Границы объектов привязываются к пиксельной сетке.
