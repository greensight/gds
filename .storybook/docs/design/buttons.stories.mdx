import { Meta } from '@storybook/addon-docs/blocks';
import Note from '../../components/Note';
import buttonThemes from '../../../src/images/docs/button-themes.png';
import buttonAutoLayout from '../../../src/images/docs/button-auto-layout.png';
import buttonFigma from '../../../src/images/docs/button-figma.png';
import buttonResult from '../../../src/images/docs/button-result.png';

<Meta title="Design | Buttons" />

# Buttons

Кнопка — компонент, который включает в себя параметры:

-   Темы (главная, второстепенная, ссылка)
-   Размер (маленький, средний, большой)
-   Блочная кнопка
-   Заблокированное состояние
-   Иконка (слева или справа от текста)
-   Иконка без текста
-   Текст в несколько строк

<Note icon="👉🏿">
    Решение поддерживает автоматический перенос длинного текста на следующую строку, если кнопка не влезает по
    горизонтали в экран.
</Note>
<br />

## Реализация в Фигме

Дизайнер рисует кнопки проекта на фрейме `Buttons` на странице `Ui-kit`.

Дизайнеру необходимо показать стилистику всех кнопок, используемых на проекте. Однако, если кнопка принадлежит теме, то есть все состояния у неё совпадают, то дизайнер может не отрисовывать отдельно иконки, блочную кнопку, кнопку с переносом строки. Фронтенд сделает это автоматически.

## Темы

Тема — это набор одинаковых стилистических решений для состояний элемента.

<img src={buttonThemes} alt="Темы primary и secondary из GDS" style={{ display: 'block', margin: '0 auto' }} />

_`primary` и `secondary` — 2 темы кнопок в GDS._

## Состояния

Всего доступно 5 состояний:

1. `default` - обычное
1. `hover` - при наведении
1. `active` - при нажатии
1. `disabled` - заблокировано
1. `focus` - фокус с клавиатуры

<Note icon="👉🏿">
    Обязательными для определения дизайнером являются 3 из них: <strong>default</strong>, <strong>hover</strong>,{' '}
    <strong>disabled</strong>. Состояние <strong>active</strong> по умолчанию совпадает с <strong>hover</strong>, а{' '}
    <strong>focus</strong> наследует состояние фокуса всего проекта (обычно прямоугольный бокс).
</Note>

## Auto Layout

Кнопки делаются в Auto Layout.

Поскольку Auto Layout может автоматически менять ширину, в кнопку лучше сразу положить иконку слева и справа от текста и скрыть. Это позволит гибко использовать компонентный подход.

Иконки и текст выравниваются по вертикальному центру, тексту задается стиль из типографики.

В настройках задается фиксированная высота, горизонтальные отступы и отступ между внутренними элементами:

<img src={buttonAutoLayout} alt="Панель Auto Layout в Figma" style={{ display: 'block', margin: '0 auto' }} />

Блочные кнопки делаются по старому принципу без технологии Autolayout.

## Размеры

Для самой часто используемой кнопки на проекте дизайнер рисует размеры. Обычно их три: маленькая, средняя и большая. Эта размерность распространится на все кнопки.

При изменении размера в кнопке могут меняться следующие стили:

1. Типографика
1. Высота
1. Горизонтальные поля
1. Размер иконки
1. Отступ между иконкой и текстом
1. Скругление

## Типографика

Для кнопки прописываются стили в разделе типографики (фрейм `Typography`). За это отвечают переменные:

1. `buttonLg` - большая
1. `buttonMd` - средняя
1. `buttonSm` - малая

## Минимальный набор

Минимальный набор компонента содержит состояния для разных тем и одно правило для всех размеров. Иконки включаются в кнопки.

<img src={buttonFigma} alt="Фрейм Buttons в Figma" style={{ display: 'block', margin: '0 auto' }} />

Из минимального набора фронтенд автоматически сделает набор вариантов, в каждый из которых будут включены состояния:<img
src={buttonResult}
alt="Множество доступных в итоге вариаций"
style={{ display: 'block', margin: '0 auto 16px' }}
/>
<br />
<Note icon="👉🏿">
    Хорошо, когда в ките присутствуют кнопки, которые используются более одного раза в проекте. Если они наследуют одну
    из тем — отрисовывать состояния для них не нужно.
</Note>
