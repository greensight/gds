import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Design / Spacings" />

# Spacings

По данным аналитики, в работе над составляющими элементами — кнопками, иконками, элементами форм — дизайнер добавляет отступы, не кратные восьми.

На более высоком уровне — при верстке блоков макета — дизайнер нуждается в куда больших отступах, чем при разработки микромодулей. Цифры могут доходить до 160 и выше.

На основе этих данных мы применили нелинейную шкалу для отступов, убрали ограничения по набору значений и сделали универсальные переменные.

![Шкалы minor и major](images/spacings/scale.png)

Шкала состоит из двух участков — `minor` и `major`. Minor имеет кратность 4px, major — 8px. Шкала major увеличивается до больших размеров и не ограничена в значениях, minor определён только до 40px.

**Общее правило:** обычно используется major, но на значениях, меньших 40px может применяться minor.

**Примеры:**

-   4px - `scale(1, true)`
-   16px - `scale(2)` или `scale(4, true)`
-   40px - `scale(5)` или `scale(10, true)`
-   64px - `scale(8)`
-   96px - `scale(12)`

👉🏿 В случае, если клиент приходит со своим дизайном, базовый шаг может быть кратным 5 или 10. В этом случае в конфигуратор заносятся значения: minor = 5, major = 10. Логика системы остается прежней.

## Ритмы

Все отступы внутри макета определяются конечными отступами от блоков и должны быть кратны четырем пикселям.

![Отступы на примере карточки товара](images/spacings/product-card.jpg)

Базовые линии текста могут не попадать в вертикальную сетку макета — это нормально. Важно сохранять отступы между элементами, кратными четырем или восьми:

![Выравнивание текста в Figma](images/spacings/text.png)

## Рекомендации

-   Соблюдайте единство в отступах в одинаковых местах.
-   Обходитесь минимальным набором необходимых переменных.
-   Не изменяйте вручную высоту текстовых блоков — это затруднит работу разработчика.
