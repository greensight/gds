import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { boolean, radios, object } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import * as Yup from 'yup';
import baseTheme from '../../utils/baseTheme';
import Button from '../../components/Button';
import centeredDecorator from '../../../.storybook/decorators/centeredDecorator';
import Radio from '.';
import Form from '../../components/Form';
import Legend from '../../components/Legend';
import ErrorIcon from '../../icons/tokens/medium/errorTriangle.svg';
import SuccessIcon from '../../icons/tokens/medium/checkCircle.svg';
import {
    RadioProps,
    RadioItemTheme,
    RadioItemLabelStates,
    RadioItemLabelThemeProperties,
    RadioItemLabelStateProperties,
    RadioItemCircleStates,
    RadioItemCircleThemeProperties,
    RadioItemCircleStateProperties,
    RadioItemSizeProperties,
} from '../../../.storybook/docgen/Radio';

<Meta title="Components / Radio" component={Radio} decorators={[centeredDecorator]} />

# Radio

<Story name="Knobs">
    <Radio
        name="radioLink"
        orientation={radios('Orientation', ['vertical', 'horizontal'], 'vertical', 'Radio props')}
        alignment={radios('Alignment', ['top', 'center', 'bottom'], 'top', 'Radio props')}
        size={radios('size', ['lg', 'md', 'sm'], 'md', 'Radio props')}
        labelRight={boolean('Label right', true, 'Radio props')}
        isOptional={boolean('Optional', true, 'Radio props')}
        defaultValue="second"
    >
        <Legend
            as="legend"
            hint="Подсказка"
            IconBefore={baseTheme.global.placeholder}
            IconAfter={baseTheme.global.placeholder}
        >
            Выберите вариант
        </Legend>
        <Radio.Item
            value="first"
            disabled={boolean('Disabled', true, 'Radio 1 props')}
            IconInner={boolean('Custom inner icon', false, 'Radio 1 props') && baseTheme.global.placeholder}
            IconOuter={boolean('Custom outer icon', false, 'Radio 1 props') && baseTheme.global.placeholder}
            __theme={
                boolean('Use custom theme', false, 'Radio props')
                    ? object('themeObj', baseTheme.components.RadioItem, 'Radio props')
                    : undefined
            }
        >
            Вариант 1
        </Radio.Item>
        <Radio.Item
            value="second"
            disabled={boolean('Disabled', false, 'Radio 2 props')}
            IconInner={boolean('Custom inner icon', false, 'Radio 2 props') && baseTheme.global.placeholder}
            IconOuter={boolean('Custom outer icon', false, 'Radio 2 props') && baseTheme.global.placeholder}
            __theme={
                boolean('Use custom theme', false, 'Radio props')
                    ? object('themeObj', baseTheme.components.RadioItem, 'Radio props')
                    : undefined
            }
        >
            Вариант 2
        </Radio.Item>
        <Radio.Item
            value="three"
            disabled={boolean('Disabled', false, 'Radio 3 props')}
            IconInner={boolean('Custom inner icon', false, 'Radio 3 props') && baseTheme.global.placeholder}
            IconOuter={boolean('Custom outer icon', false, 'Radio 3 props') && baseTheme.global.placeholder}
            css={{ maxWidth: '170px' }}
            __theme={
                boolean('Use custom theme', false, 'Radio props')
                    ? object('themeObj', baseTheme.components.RadioItem, 'Radio props')
                    : undefined
            }
        >
            Вариант 3 с длинным текстом для тестирования
        </Radio.Item>
    </Radio>
</Story>

Компонент `Radio` представляет собой группу радиобаттонов, построенную через `fieldset`. Отдельные радиобаттоны передаются в качестве children через `Radio.Item`. Вся группа имеет единый `name` и каждый item имеет уникальный `value`. Результат представляется в виде выбранного value.

В качестве радиобаттонов используются настоящие input type="radio", стилизация которых происходит через псевдоэлементы. Сам инпут визуально скрывается, а стилизуется label, где бокс - это ::before, марка - ::after. Это работает благодаря тому, что клик по label триггерит изменение инпута.

В качестве бокса и марки можно использовать кастомные SVG-иконки.

Радиокнопки определяются полем `RadioItem` в объекте темы. В целом концепт таков, что настройки диктуются сочетанием темы со стилистическими настройками из `theme` и размера из `sizes`. Тема содержит поля для стилизации составляющих `Radio.Item`:

-   `label` – поле для стилизации лейбла;
-   `outer` – поле для стилизации бокса;
-   `inner` – поле для стилизации марки;

После определения темы и размера для радиокнопок становятся доступны вариации, задаваемые через props: `orientation`, `alignment`, `disabled` и т.д. Детальнее доступные props разобраны в разделе `Use cases`.

❗️♿️ Т.к. используется fieldset, то разметка должна содержать legend. Для гибкой настройки сетки легенда не "зашита" внутри Radio, её нужно добавить самостоятельно, используя компонент `Legend`. В идеале легенда должны быть видимой, но при необходимости её можно визуально скрыть, оставив доступной для AT, через проп Legend `hiddenLegend`.

🏆 Наиболее вероятная для выбора опция должна идти на первом месте и при этом должна быть предвыбрана. Это позволит пользователю быстрее заполнить форму и предостережет его от ошибок.

❗️ При задании темы нужно следить, чтобы бокс и марка радиобаттона были внешне похожи на радиобаттон. 📚[THINGS TO AVOID: Using radio buttons that look like checkboxes](https://s3.us-west-2.amazonaws.com/secure.notion-static.com/807a0510-222e-43ed-8822-b67261a1db70/form-design-patterns.pdf?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Credential=AKIAT73L2G45O3KS52Y5%2F20200428%2Fus-west-2%2Fs3%2Faws4_request&X-Amz-Date=20200428T084900Z&X-Amz-Expires=86400&X-Amz-Signature=a1e1e66b0a1b275b4bd51a77b056944f2d496221369631d4bb394f344fd8aaf6&X-Amz-SignedHeaders=host&response-content-disposition=filename%20%3D%22PDF.pdf%22)

## Props

<Props components={{ Radio: RadioProps, 'Radio.Item': Radio.Item }} />

## Theme

<Props
    components={{
        RadioItemTheme,
        RadioItemLabelStates,
        RadioItemLabelThemeProperties,
        RadioItemLabelStateProperties,
        RadioItemSizeProperties,
        RadioItemCircleStates,
        RadioItemCircleThemeProperties,
        RadioItemCircleStateProperties,
    }}
/>

## Use cases

### Basic

Компонент Radio без параметров рендерит `Radio.Item` в колонку, бокс выровнен по центру первой строки, размер "md".

<Preview>
    <Story name="Basic">
        <Radio name="basic" defaultValue="basic-first">
            <Radio.Item value="basic-first">Вариант 1</Radio.Item>
            <Radio.Item value="basic-second">Вариант 2</Radio.Item>
        </Radio>
    </Story>
</Preview>

### Sizes

Размеры для легенды определяются в объекте темы в поле `RadioItem.sizes`. Присвоенные имена становятся доступны через проп `size`. Размер задается `Radio`, а `Radio.Item` получают его через контекст.

**Рекомендуемые имена:** 'lg', 'md', 'sm'.

<Preview>
    <Story name="Large size">
        <Radio name="large-size" size="lg" defaultValue="large-first">
            <Radio.Item value="large-first">Вариант 1</Radio.Item>
            <Radio.Item value="large-second">Вариант 2</Radio.Item>
        </Radio>
    </Story>
    <Story id="components-radio--basic" />
    <Story name="Small size">
        <Radio name="small-size" size="sm" defaultValue="small-first">
            <Radio.Item value="small-first">Вариант 1</Radio.Item>
            <Radio.Item value="small-second">Вариант 2</Radio.Item>
        </Radio>
    </Story>
</Preview>

### Orientation

`Radio.Item` могут располагаться в колонку, а могут в ряд. Для управления потоком `Radio.Item` получает проп `orientation` через контекст `Radio`. По умолчанию проп имеет значение `vertical`.

<Preview>
    <Story id="components-radio--basic" />
    <Story name="Horizontal">
        <Radio name="horizontal" orientation="horizontal" defaultValue="orientation-first">
            <Radio.Item value="orientation-first">Вариант 1</Radio.Item>
            <Radio.Item value="orientation-second">Вариант 2</Radio.Item>
        </Radio>
    </Story>
</Preview>

### Alignment

Бокс у `Radio.Item` можно выровнять по первой или последней строке, а также по центру лейбла. Для управления вертикальным выравниванием бокса `Radio.Item` получает проп `alignment` через контекст `Radio`. По умолчанию проп имеет значение `top`.

❗️ Расположение бокса по базовой линии текста предпочтительней, так как такой бокс и его состояние сразу видно при чтении опции.

<Preview>
    <Story name="Alignment top">
        <Radio name="alignment-top" css={{ maxWidth: '30vw' }} defaultValue="alignment-t-first">
            <Radio.Item value="alignment-t-first">Вариант 1</Radio.Item>
            <Radio.Item value="alignment-t-second">
                Вариант 2 с длинным текстом, который будет пересен на 2 строки, а может и больше
            </Radio.Item>
        </Radio>
    </Story>
    <Story name="Alignment center">
        <Radio name="alignment-center" alignment="center" css={{ maxWidth: '30vw' }} defaultValue="alignment-c-first">
            <Radio.Item value="alignment-c-first">Вариант 1</Radio.Item>
            <Radio.Item value="alignment-c-second">
                Вариант 2 с длинным текстом, который будет пересен на 2 строки, а может и больше
            </Radio.Item>
        </Radio>
    </Story>
    <Story name="Alignment bottom">
        <Radio name="alignment-bottom" alignment="bottom" css={{ maxWidth: '30vw' }} defaultValue="alignment-b-first">
            <Radio.Item value="alignment-b-first">Вариант 1</Radio.Item>
            <Radio.Item value="alignment-b-second">
                Вариант 2 с длинным текстом, который будет пересен на 2 строки, а может и больше
            </Radio.Item>
        </Radio>
    </Story>
</Preview>

### Label position

Горизонтальным расположением бокса управляет проп `labelRight` для `Radio`. По умолчанию бокс находится слева от лейбла.

❗️ Расположение бокса слева от текста предпочтительней, так как это делает интерфейс более знакомым для восприятия.

<Preview>
    <Story name="Label position">
        <Radio name="label-position" labelRight={false} defaultValue="label-position-first">
            <Radio.Item value="label-position-first">Вариант 1</Radio.Item>
            <Radio.Item value="label-position-second">Вариант 2</Radio.Item>
        </Radio>
    </Story>
    <Story id="components-radio--basic" />
</Preview>

### Custom box

Для `Radio.Item` есть возможность использовать SVG-иконки вместо стандартных псевдоэлементов для обозначения бокса и марки.

❗️ Используя костомные иконки, следите за тем, чтобы радиобаттон оставался похожим на радиобаттон. Бокс и марка должны быть круглыми и хорошо различимыми. При использовании SVG-иконок накладываются ограничения на тени: тень для SVG формируется через свойство `filter` и не может быть внутренней.

<Preview>
    <Story name="Custom mark">
        <Radio name="custom-mark" defaultValue="custom-mark-first">
            <Radio.Item value="custom-mark-first" IconInner={baseTheme.global.placeholder}>
                Вариант 1
            </Radio.Item>
            <Radio.Item value="custom-mark-second" IconInner={baseTheme.global.placeholder}>
                Вариант 2
            </Radio.Item>
        </Radio>
    </Story>
    <Story name="Custom box">
        <Radio name="custom-box" defaultValue="custom-box-first">
            <Radio.Item value="custom-box-first" IconOuter={baseTheme.global.placeholder}>
                Вариант 1
            </Radio.Item>
            <Radio.Item value="custom-box-second" IconOuter={baseTheme.global.placeholder}>
                Вариант 2
            </Radio.Item>
        </Radio>
    </Story>
    <Story name="Custom radio">
        <Radio name="custom-radio" defaultValue="custom-radio-first">
            <Radio.Item
                value="custom-radio-first"
                IconInner={baseTheme.global.placeholder}
                IconOuter={baseTheme.global.placeholder}
            >
                Вариант 1
            </Radio.Item>
            <Radio.Item
                value="custom-radio-second"
                IconInner={baseTheme.global.placeholder}
                IconOuter={baseTheme.global.placeholder}
            >
                Вариант 2
            </Radio.Item>
        </Radio>
    </Story>
</Preview>

### With Form

<Preview>
    <Story name="Inside Form">
        {() => {
            const initialValues = {
                formRadio1: '',
                formRadio2: 'formRadio2-third',
            };
            const validationSchema = Yup.object().shape({
                formRadio1: Yup.string().required('Обязательное поле'),
                formRadio2: Yup.string().required('Обязательное поле'),
            });
            return (
                <Form
                    initialValues={initialValues}
                    validationSchema={validationSchema}
                    onSubmit={action('Submit')}
                    style={{ width: '25vw', minWidth: '320px' }}
                    ErrorIcon={ErrorIcon}
                    SuccessIcon={SuccessIcon}
                    showSuccess={boolean('showSuccess', true, 'Form')}
                    requiredRule={radios('requiredRule', ['optional', 'mark'], 'mark', 'Form')}
                    errorPosition={radios('errorPosition', ['top', 'bottom'], 'top', 'Form')}
                >
                    <Form.Field
                        controlId="formRadio1"
                        hint="Вариант не предвыбран"
                        hintPosition={radios('Hint position', ['top', 'bottom'], 'top', 'Field 1')}
                        size={radios('size', ['lg', 'md', 'sm'], 'md', 'Field 1')}
                        validationPosition={radios(
                            'validationPosition',
                            ['labelBefore', 'labelAfter'],
                            'labelAfter',
                            'Field 1',
                        )}
                    >
                        <Radio
                            orientation={radios('Orientation', ['vertical', 'horizontal'], 'vertical', 'Radio 1')}
                            alignment={radios('Alignment', ['top', 'center', 'bottom'], 'top', 'Radio 1')}
                        >
                            <Legend
                                as="legend"
                                hiddenLegend={boolean('Hidden Legend', false, 'Legend 1')}
                                IconBefore={
                                    boolean('Legend icon before', true, 'Legend 1') && baseTheme.global.placeholder
                                }
                                IconAfter={
                                    boolean('Legend icon after', true, 'Legend 1') && baseTheme.global.placeholder
                                }
                                block={boolean('Block', false, 'Legend 1')}
                                horizontalAlignment={radios(
                                    'Horizontal align',
                                    ['left', 'center', 'right'],
                                    'left',
                                    'Legend 1',
                                )}
                            >
                                Выберите вариант
                            </Legend>
                            <Radio.Item value="formRadio1-first">Вариант 1</Radio.Item>
                            <Radio.Item value="formRadio1-second">Вариант 2</Radio.Item>
                            <Radio.Item value="formRadio1-third" disabled>
                                Вариант 3 с длинным текстом для тестирования вертикального выравнивания бокса
                            </Radio.Item>
                        </Radio>
                    </Form.Field>
                    <br />
                    <Form.Field
                        controlId="formRadio2"
                        hint="Вариант предвыбран"
                        size={radios('size', ['lg', 'md', 'sm'], 'md', 'Field 2')}
                    >
                        <Radio
                            orientation={radios('Orientation', ['vertical', 'horizontal'], 'vertical', 'Radio 2')}
                            alignment={radios('Alignment', ['top', 'center', 'bottom'], 'top', 'Radio 2')}
                        >
                            <Legend
                                as="legend"
                                hiddenLegend={boolean('Hidden Legend', false, 'Legend 2')}
                                IconBefore={
                                    boolean('Legend icon before', true, 'Legend 2') && baseTheme.global.placeholder
                                }
                                IconAfter={
                                    boolean('Legend icon after', true, 'Legend 2') && baseTheme.global.placeholder
                                }
                                block={boolean('Block', false, 'Legend 2')}
                                horizontalAlignment={radios(
                                    'Horizontal align',
                                    ['left', 'center', 'right'],
                                    'left',
                                    'Legend 2',
                                )}
                            >
                                Выберите вариант
                            </Legend>
                            <Radio.Item value="formRadio2-first">Варинат 1</Radio.Item>
                            <Radio.Item value="formRadio2-second">Варинат 2</Radio.Item>
                            <Radio.Item value="formRadio2-third" disabled>
                                Вариант 3 с длинным текстом для тестирования вертикального выравнивания бокса
                            </Radio.Item>
                        </Radio>
                    </Form.Field>
                    <br />
                    <Button type="submit">Submit</Button>
                </Form>
            );
        }}
    </Story>
</Preview>
