import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { text, boolean, radios, object } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import * as Yup from 'yup';
import baseTheme from '../../utils/baseTheme';
import Button from '../../components/Button';
import centeredDecorator from '../../../.storybook/decorators/centeredDecorator';
import Radio from '.';
import Form from '../../components/Form';
import Legend from '../../components/Legend';
import ErrorIcon from '../../icons/tokens/medium/errorTriangle.svg';

<Meta title="Components / Radio" component={Radio} decorators={[centeredDecorator]} />

# Radio

<Story name="Knobs">
    <Radio
        name="radioLink"
        orientation={radios('Orientation', ['vertical', 'horizontal'], 'vertical', 'Radio props')}
        alignment={radios('Alignment', ['top', 'center', 'bottom'], 'top', 'Radio props')}
        size={radios('size', ['lg', 'md', 'sm'], 'md', 'Radio props')}
        isOptional={boolean('Optional', true, 'Radio props')}
    >
        <Legend
            as="legend"
            hint="Подсказка"
            IconBefore={baseTheme.global.placeholder}
            IconAfter={baseTheme.global.placeholder}
        >
            Выберите вариант
        </Legend>
        <Radio.Item
            value="first"
            labelRight={boolean('Label right', true, 'Radio 1 props')}
            disabled={boolean('Disabled', true, 'Radio 1 props')}
            IconInner={boolean('Custom inner icon', false, 'Radio 1 props') && baseTheme.global.placeholder}
            IconOuter={boolean('Custom outer icon', false, 'Radio 1 props') && baseTheme.global.placeholder}
            __theme={
                boolean('Use custom theme', false, 'Radio 1 props')
                    ? object('themeObj', baseTheme.components.RadioItem, 'Radio 1 props')
                    : undefined
            }
        >
            Вариант 1
        </Radio.Item>
        <Radio.Item value="second" defaultChecked>
            Вариант 2
        </Radio.Item>
        <Radio.Item value="three">Вариант 3 с длинным текстом</Radio.Item>
    </Radio>
</Story>

Компонент `Radio` представляет собой группу радиобаттонов, построенную через fieldset. Отдельные радиобаттоны передаются в качестве children через `Radio.Item`. Вся группа имеет единый `name` и каждый item имеет уникальный `value`. Результат представляется в виде выбранного value.

В качестве радиобаттонов используются настоящие input type="radio", стилизация которых происходит через псевдоэлементы. Сам инпут визуально скрывается, а стилизуется label, где бокс - это ::before, марка - ::after. Это работает благодаря тому, что клик по label триггерит изменение инпута.

☝🏿 Т.к. используется fieldset, то разметка должна содержать legend - для этого предусмотрен обязательный проп `legend`. В идеале легенда должны быть видимой, но при необходимости её можно визуально скрыть, оставив доступной для AT, через проп `isHiddenLegend`.

## Props

<Props of={Radio} />

## Use cases

### Basic

Компонент Radio без параметров рендерит `Radio.Item` в колонку, иконка выровнена по центру первой строки, размер "md".

<Preview>
    <Story name="Basic">
        <Radio name="basic">
            <Radio.Item value="basic-first">Вариант 1</Radio.Item>
            <Radio.Item value="basic-second" defaultChecked>
                Вариант 2
            </Radio.Item>
        </Radio>
    </Story>
</Preview>

### Sizes

Размеры для легенды определяются в объекте темы в поле `RadioItem.sizes`. Присвоенные имена становятся доступны через проп `size`. Размер задается `Radio`, а `Radio.Item` получают его через контекст.

**Рекомендуемые имена:** 'lg', 'md', 'sm'.

<Preview>
    <Story name="Large size">
        <Radio name="Large size" size="lg">
            <Radio.Item value="large-first">Вариант 1</Radio.Item>
            <Radio.Item value="large-second" defaultChecked>
                Вариант 2
            </Radio.Item>
        </Radio>
    </Story>
    <Story id="components-radio--basic" />
    <Story name="Small size">
        <Radio name="Small size" size="sm">
            <Radio.Item value="small-first">Вариант 1</Radio.Item>
            <Radio.Item value="small-second" defaultChecked>
                Вариант 2
            </Radio.Item>
        </Radio>
    </Story>
</Preview>

### Orientation

`Radio.Item` могут располагаться в колонку, а могут в ряд. Для управления потоком `Radio.Item` получает проп `orientation` через контекст `Radio`. По умолчанию проп имеет значение `vertical`.

<Preview>
    <Story id="components-radio--basic" />
    <Story name="Horizontal">
        <Radio name="Horizontal" orientation="horizontal">
            <Radio.Item value="orientation-first">Вариант 1</Radio.Item>
            <Radio.Item value="orientation-second" defaultChecked>
                Вариант 2
            </Radio.Item>
        </Radio>
    </Story>
</Preview>

### Alignment

Иконку радио у `Radio.Item` можно выровнять по первой или последней строке, а также по центру блока с текстом. Для управления выравниванием иконки `Radio.Item` получает проп `фдшптьуте` через контекст `Radio`. По умолчанию проп имеет значение `top`.

<Preview>
    <Story name="Alignment top">
        <Radio name="Alignment top">
            <Radio.Item value="alignment-t-first">Вариант 1</Radio.Item>
            <Radio.Item value="alignment-t-second" defaultChecked>
                Вариант 2 с длинным текстом, который будет пересен на 2 строки, а может и больше
            </Radio.Item>
        </Radio>
    </Story>
    <Story name="Alignment center">
        <Radio name="Alignment center" alignment="center">
            <Radio.Item value="alignment-c-first">Вариант 1</Radio.Item>
            <Radio.Item value="alignment-c-second" defaultChecked>
                Вариант 2 с длинным текстом, который будет пересен на 2 строки, а может и больше
            </Radio.Item>
        </Radio>
    </Story>
    <Story name="Alignment bottom">
        <Radio name="Alignment bottom" alignment="bottom">
            <Radio.Item value="alignment-b-first">Вариант 1</Radio.Item>
            <Radio.Item value="alignment-b-second" defaultChecked>
                Вариант 2 с длинным текстом, который будет пересен на 2 строки, а может и больше
            </Radio.Item>
        </Radio>
    </Story>
</Preview>

### With Form

<Preview>
    <Story name="With Form">
        {() => {
            const initialValues = {
                radioKnobs: '',
            };
            const validationSchema = Yup.object().shape({
                radioKnobs: Yup.string().required('Обязательное поле'),
            });
            return (
                <Form
                    initialValues={initialValues}
                    validationSchema={validationSchema}
                    onSubmit={action('Submit')}
                    style={{ width: '25vw', minWidth: '320px' }}
                    ErrorIcon={ErrorIcon}
                    required="mark"
                    errorPosition={radios('errorPosition', ['top', 'bottom'], 'top', 'Form')}
                >
                    <Form.Field
                        controlId="radioKnobs"
                        hint="Подсказка 1"
                        hiddenLabel={boolean('Hidden Legend', false, 'Field')}
                        size={radios('size', ['lg', 'md', 'sm'], 'md', 'Field')}
                    >
                        <Form.Radio
                            orientation={radios('Orientation', ['vertical', 'horizontal'], 'vertical', 'Radio')}
                            alignment={radios('Alignment', ['top', 'center', 'bottom'], 'top', 'Radio')}
                        >
                            <Form.Label
                                as="legend"
                                IconBefore={baseTheme.global.placeholder}
                                IconAfter={baseTheme.global.placeholder}
                            >
                                Выберите вариант
                            </Form.Label>
                            <Radio.Item
                                value="first"
                                disabled={boolean('Disabled', true, 'Radio 1 props')}
                                IconInner={
                                    boolean('Custom inner icon', false, 'Radio 1 props') && baseTheme.global.placeholder
                                }
                            >
                                {text('Label', 'Вариант 1', 'Radio 1 props')}
                            </Radio.Item>
                            <Radio.Item
                                value="second"
                                disabled={boolean('Disabled', false, 'Radio 2 props')}
                                IconInner={
                                    boolean('Custom inner icon', false, 'Radio 2 props') && baseTheme.global.placeholder
                                }
                            >
                                {text(
                                    'Label',
                                    'Вариант с очень длинным названием, который, осевидно, займёт две строки или даже больше',
                                    'Radio 2 props',
                                )}
                            </Radio.Item>
                            <Radio.Item value="third" disabled={boolean('Disabled', false, 'Radio 3 props')}>
                                {text('Label', 'Вариант 3', 'Radio 3 props')}
                            </Radio.Item>
                            <Radio.Item value="fourth" disabled={boolean('Disabled', false, 'Radio 4 props')}>
                                {text('Label', 'Вариант 4', 'Radio 4 props')}
                            </Radio.Item>
                        </Form.Radio>
                    </Form.Field>
                    <br />
                    <Button type="submit">Submit</Button>
                </Form>
            );
        }}
    </Story>
</Preview>
