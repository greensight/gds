import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Item from '../../../.storybook/components/Item';
import Container from '.';

<Meta title="Components | Layout / Container" component={Container} />

# Container

<Preview>
    <Story name="Basic">
        <Container>
            <Item>Content</Item>
        </Container>
    </Story>
</Preview>

`Container` - это обёртка для ограничения контента по ширине. Этот компонент стилизован через значения ширины контейнера и полей, полученных через токены и хранящихся в объекте темы.

Контейнер используется в компоненте `Section`, но может использоваться и напрямую в других местах, где тег section не нужен.

❗️ В документации используется компонент `Item` чисто в демонстративных целях - визуально показать бокс контента. Это вспомогательный компонент и на проекте его не будет - будет ваш контент.

## Props

<Props of={Container} />
