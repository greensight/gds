import { Meta, Preview, Props, Story } from '@storybook/addon-docs';

import { Section } from '.';
import { Placeholder } from '../../../helpers/common/Placeholder';
import { scale } from '../../../utils/common/scale';
import { SectionProps } from '../../../../.storybook/docgen/SectionScss';

<Meta title="Components / Scss / Helpers / Section" component={Section} />

# Section

<Story
    name="Knobs"
    args={{
        container: true,
    }}
>
    {(args) => (
        <Section {...args}>
            <Placeholder>Here goes section content</Placeholder>
        </Section>
    )}
</Story>

Обычно при разметке основного контента страницы он делится на разделы через тег section. Для упрощения этого процесса предусмотрен компонент `Section`.

💬 В документации используется компонент `Placeholder` чисто в демонстративных целях – визуально показать бокс контента. Это вспомогательный компонент GDS и на проекте его не будет.

## Props

<Props components={{ Section: SectionProps }} />

## Use cases

### Container

Внутри каждого раздела может быть или не быть контейнер.

Величина контейнера, поля и отступы собираются с помощью миксина `@container`.

<Preview>
    <Story name="Container">
        <>
            <Section style={{ marginBottom: scale(2) }}>
                <Placeholder>Section with container</Placeholder>
            </Section>
            <Section container={false}>
                <Placeholder>Section without container</Placeholder>
            </Section>
        </>
    </Story>
</Preview>
