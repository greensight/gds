import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { text, radios, boolean, object } from '@storybook/addon-knobs';
import centeredDecorator from '../../../.storybook/decorators/centeredDecorator';
import Legend from '.';
import Form from '../../components/Form';
import baseTheme from '../../utils/baseTheme';

<Meta title="Components / Legend" component={Legend} decorators={[centeredDecorator]} />

# Legend

Легенда...

<Preview>
    <Story name="Knobs">
        <Legend
            as={text('Tag', 'legend')}
            name="Example"
            hint={text('Hint', 'Подсказка к полю')}
            hiddenLegend={boolean('Hidden legend', false)}
            size={radios('size', ['lg', 'md', 'sm'], 'md')}
            optional={boolean('Is optional?', true) ? text('Optional text', '(необязательное поле)') : undefined}
            IconBefore={boolean('Icon before', true) && baseTheme.global.placeholder}
            IconAfter={boolean('Icon after', true) && baseTheme.global.placeholder}
            __theme={
                boolean('Use custom theme', false) ? object('themeObj', baseTheme.components.FormLabel) : undefined
            }
            __hintTheme={
                boolean('Use custom hint theme', false)
                    ? object('hintThemeObj', baseTheme.components.FormHint)
                    : undefined
            }
        >
            Пример лейбла/легенды
        </Legend>
    </Story>
</Preview>

<Preview>
    <Story name="Inside Form">
        <Form
            style={{ width: '25vw', minWidth: '320px' }}
            required={radios('required', ['optional', 'mark'], 'optional', 'Form Props')}
        >
            <Form.Field
                controlId="field-1"
                optional={
                    boolean('Is optional?', true, 'Field Props')
                        ? text('Optional text', '(Необязательное поле)', 'Field Props')
                        : undefined
                }
                hint={boolean('Hint', true, 'Field Props') ? text('Hint text', 'Подсказка', 'Field Props') : undefined}
                size={radios('size', ['lg', 'md', 'sm'], 'md', 'Field Props')}
                hiddenLabel={boolean('Hidden label', false, 'Field Props')}
            >
                <Form.Label
                    as={text('Tag', 'Legend', 'Label Props')}
                    IconBefore={boolean('Label icon before', true, 'Label Props') && baseTheme.global.placeholder}
                    IconAfter={boolean('Label icon after', true, 'Label Props') && baseTheme.global.placeholder}
                >
                    Лейбл внутри Form
                </Form.Label>
            </Form.Field>
        </Form>
    </Story>
</Preview>

## Props

### Legend

<Props of={Legend} />
