import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { text, radios } from '@storybook/addon-knobs';
import centeredDecorator from '../../../.storybook/decorators/centeredDecorator';
import Legend from '.';
import Form from '../../components/Form';

<Meta title="Components / Legend" component={Legend} decorators={[centeredDecorator]} />

# Legend

Легенда...

<Preview>
    <Story name="Knobs">
        <Form
            style={{ width: '25vw', minWidth: '320px' }}
            required={radios('required', ['optional', 'mark'], 'optional')}
        >
            <Form.Field
                controlId="field-1"
                hint={text('Hint', 'Подсказка')}
                optional="(Необязательно)"
                size={radios('size', ['lg', 'md', 'sm'], 'md')}
            >
                <Legend as={text('Tag', 'fieldset', 'Props')} label={text('Label', 'Пример текста', 'Props')} />
            </Form.Field>
            <Form.Field
                controlId="field-2"
                hint={text('Hint', 'Подсказка')}
                size={radios('size', ['lg', 'md', 'sm'], 'md')}
            >
                <Legend as={text('Tag', 'fieldset', 'Props')} label={text('Label', 'Пример текста', 'Props')} />
            </Form.Field>
        </Form>
    </Story>
</Preview>

## Props

### Legend

<Props of={Legend} />
