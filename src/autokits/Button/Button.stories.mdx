import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Button from '.';

<Meta title="Autokits / Button" component={Button} />

# Button

<Preview>
    <Story name="basic">
        <Button />
    </Story>
</Preview>

Автокит для компонента `Button`. Читает содержимое поля `components.button` объекта темы.

## Props

<Props of={Button} />
