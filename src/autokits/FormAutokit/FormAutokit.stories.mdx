import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import paddingDecorator from '../../../.storybook/decorators/paddingDecorator';
import { action } from '@storybook/addon-actions';
import FormAutokit from '.';
import ErrorIcon from '../../icons/tokens/medium/errorTriangle.svg';
import SuccessIcon from '../../icons/tokens/medium/checkCircle.svg';

<Meta title="Autokits / FormAutokit" component={FormAutokit} decorators={[paddingDecorator]} />

# Form

<Preview>
    <Story name="basic">
        <FormAutokit SuccessIcon={SuccessIcon} ErrorIcon={ErrorIcon} onSubmit={action('Submit')} />
    </Story>
</Preview>

## Props

<Props of={FormAutokit} />
