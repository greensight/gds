/// <reference types="react" />
export interface ShadowProps {
    /** Shadow variable name. */
    name: string;
    /** Shadow value. */
    value: string;
}
export declare const Shadow: ({ name, value }: ShadowProps) => JSX.Element;
export default Shadow;
